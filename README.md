# cnn-classification-project
CNN models built with TensorFlow/Keras to classify images from Fashion-MNIST and tabular data from the Iris dataset, demonstrating the versatility of convolutional networks across different data types.
